import React, { ReactElement } from 'react'

interface Props {
    
}

export default function NotFoundView({}: Props): ReactElement {
    return (
        <div>
            Error: Not Found
        </div>
    )
}
