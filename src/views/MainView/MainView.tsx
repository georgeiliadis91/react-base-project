import React, { ReactElement } from 'react'

interface Props {
    
}

export default function MainView({}: Props): ReactElement {
    return (
        <div>
            MainView - Component
        </div>
    )
}
